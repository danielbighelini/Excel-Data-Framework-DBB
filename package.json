{
  "scripts": {
    "release:dry": "dotenv -e .env -- semantic-release --dry-run"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "dotenv-cli": "^10.0.0",
    "semantic-release": "^24.2.9"
  }
}
