Name: FX.PARAMETRO
Scope: Workbook
Source Workbook: Excel Data Framework DBB.xlsx
Source Workbook Folder: Excel Data Framework DBB__xlsx
Description: Retorna o valor de um parÃ¢metro da tabela tbParametros a partir do seu nome. Opcionalmente permite informar um valor padrÃ£o, utilizado quando o parÃ¢metro nÃ£o Ã© encontrado. Retorna #N/D caso nÃ£o exista e nenhum padrÃ£o seja informado.


Formula:
=LAMBDA(parametro,padrao,LET(_Posicao, XMATCH(parametro, tbParametros[Parâmetro], 0), IFERROR(INDEX(tbParametros[Valor], _Posicao), IF(ISOMITTED(padrao), NA(), padrao))))

Localized Formula (pt-BR) - Preview Only:
=LAMBDA(parametro,padrao,LET(_Posicao, CORRESPX(parametro, tbParametros[Parâmetro], 0), SEERRO(ÍNDICE(tbParametros[Valor], _Posicao), SE(ISOMITTED(padrao), NÃO.DISP(), padrao))))

Raw Formula Snapshot (en-US) - Preview Only:
=_xlfn.LAMBDA(_xlpm.parametro,_xlop.padrao,_xlfn.LET(_xlpm._Posicao, _xlfn.XMATCH(_xlpm.parametro, tbParametros[Parâmetro], 0), IFERROR(INDEX(tbParametros[Valor], _xlpm._Posicao), IF(_xlfn.ISOMITTED(_xlpm.padrao), NA(), _xlpm.padrao))))
